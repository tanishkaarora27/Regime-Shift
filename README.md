# Regime-Shift
This project tests a simple but important idea: if a machine learning model can predict when the stock market is about to become volatile, can that prediction be used to shift a portfolio into safer assets at the right time — and does doing so actually make the portfolio better? 
